# markdown: publish ONLY the README — everything else (GRANT.md, notes, drafts) stays private
*.md
!README.md

# secrets — the OpenAI key for brief.js lives here and must never be published
.env

# misc
*.bak
node_modules/
.DS_Store
Thumbs.db
